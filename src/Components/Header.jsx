import React from "react";
import Logo from './../Logo_main.png'

const Header = () => {
    return (
        <header className='header'>
            <img src={Logo}></img>
        </header>
    )
}

export default Header;